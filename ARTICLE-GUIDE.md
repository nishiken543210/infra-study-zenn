# 記事テンプレート & 粒度ガイド（infra-study Zenn）

infra-study のおまけ記事を **同じ粒度・同じ型** で量産するための規格。
人間も Codex もこの1ファイルに従って書く。基準記事は `articles/proxmox-linux-disk-serial-mapping.md`。

> 目的：①更新頻度を上げる（本数を増やす）②全記事を将来 **Part / Chapter 単位でまとめ直せる**（総集編・書籍化）ように、各記事へ機械可読のメタを持たせる。

---

## 0. 1記事の粒度（最重要）

- **1記事 = 章の中の「1つの山場・1つの判断/作業」**。テーマを広げない。
  - ✅「同容量ディスクが5本あるとき、拡張対象をどう特定するか」
  - ❌「Proxmoxのディスク管理ぜんぶ」
- 作業時間にして **10〜30分相当** の、自己完結した problem → solution。
- **マニアック寄りOK**（"インフラでも意外と知らない" ニッチを歓迎）。
- 文量目安：**markdown 300〜400行**（frontmatter＋本文＋検証ログ＋フッター込み）。

---

## 1. frontmatter 規格

```yaml
---
title: "（下の「タイトルの型」に従う）"
emoji: "💽"                 # 記事内容に合う絵文字1つ
type: "tech"
topics: ["Linux", "server", "infra", "初心者", "command"]  # 最大5・公開表示される
published: false           # 下書き=false。公開判断はオーナー
# --- 書籍化メタ（Zennは無視・まとめ抽出専用）---
book_part: 1               # PART番号（整数）
book_chapter: 1            # CHAPTER番号（整数）
book_order: 3              # 章内の並び順（整数・まとめ時の章内順序）
book_unit: "linux-basics"  # まとめ単位スラッグ（任意・章をまたぐ束ね用）
---
```

- `book_*` は **Zenn が解釈しない独自キー**（公開に影響しない・無視される）。これで後から Part/Chapter を割り出せる。
- 本編の章に紐づかない集客記事などは `book_part: 0 / book_chapter: 0`、`book_unit` で分類（例 `intro`）。
- **抽出例**（まとめ時）：
  ```bash
  # 全記事の Part/Chapter/順序を一覧
  grep -rl '^book_part:' articles/ | while read f; do
    awk -F': ' '/^book_part:/{p=$2}/^book_chapter:/{c=$2}/^book_order:/{o=$2}/^title:/{t=$0}
      END{printf "P%s C%s #%s  %s  %s\n",p,c,o,FILENAME,t}' "$f"
  done | sort
  ```

---

## 2. タイトルの型

第一候補は、実際にやったことをそのまま入口にする**体験・制作ログ型**とする。

### 判断の根拠（2026-08-03）

記事16本を棚卸しした結果は次のとおり。

| 系統 | 本数 | 公開済み | PV 実績 |
|---|---:|---:|---|
| 体験・制作ログ型（「〜した話」「〜作った」「〜気づいた」「〜理由」） | 11本 | 8本すべてこちら | 79PV / 65PV / 17PV |
| 「〜でも〜」型 | 5本 | 0本 | 実績なし |

実績があるのは体験・制作ログ型だけで、「〜でも〜」型はまだ公開されていない。Zenn 全体でも「〜してみた」「〜を作ってみた」という体験の共有が主流だとオーナーが観察しているため、現時点では体験・制作ログ型を第一候補にする。

ただし、この表だけでタイトル型の優劣は比較できない。記事間の PV 差は、Proxmox・自宅サーバーなどの題材は需要が厚く、TrueNAS・Cloudflare Workers などは薄いことが主因と見ている。タイトル型だけの効果は分離できていない。

### 第一候補：体験・制作ログ型

実際の状況、行動、結果のうち、記事の山場が伝わるものを組み合わせる。主に次の3パターンを使う。

- **「〜した話」型**：`[立場・状況]が[対象]を[行動]した話`
  - 例：インフラ経験者が初めて自宅サーバーを買って Proxmox を入れた話
  - 例：自宅サーバーのディスクを「本物のストレージ」にする ── TrueNAS で iSCSI に挑んでハマった話
- **「〜を作った / 作り始めた理由」型**：`[対比・課題]、[場所・対象]に[成果物]を作った / 作り始めた理由`
  - 例：クラウドじゃなく、自宅のサーバーに Linux 学習プラットフォームを作り始めた理由
  - 例：なぜiLOがあるのにWake-on-LANが要るのか——自宅サーバーを節電運用しながら無人起動する仕組みを作った
- **「〜してみた / 〜を調べてみた」型**：`[対象]を[具体的な規模・条件]で〜してみた / 調べてみた`
  - 例：PowerShell スクリプトの文字コードを26本調べてみた
  - 結果を出すかどうかは、それが読む理由になるかで決める。驚きがフックになるなら、結果を出してよい。ただし、2つ以上の結果を並べない。`── 壊れていたのは1本、直したのは9本` は結果を2つ並べたことで冗長になり、これが「余計」と言われた原因だった。
  - 「26本調べた」は調査規模、「壊れていたのは1本」は結果として切り分ける。ただし、この切り分けは結果を伏せるためではなく、どちらを読む理由として使うか判断するために行う。
  - 実例では、結果を外して `PowerShell スクリプトの文字コードを26本調べてみた` に変えたところ、整合性は取れたが作業報告になり、読む理由が消えた。結果を全部伏せればよいわけではない。

### 第二候補：「〜でも〜」型（実績は未検証）

> `[読者像]でも[意外性]。[具体的な状況・痛み]。[対象コマンド/技術]。`

- `[読者像]`：インフラエンジニア / 駆け出しインフラ など
- `[意外性]`：迷う / 意外と知らない / 説明できない / つまずく
- `[状況]`：具体的なシーン1つ（数字や現場感を入れる）
- 例：
  - インフラエンジニアでも迷う。100GBの仮想ディスクが5本あるとき、Proxmoxで拡張対象をどう特定する？
  - インフラエンジニアでも意外と知らない。ネットが使えない現場で頼れるLinuxの`man`コマンド。

この型は5本あるが、公開済みの記事がなく、PV 実績は未検証である。使う場合は体験・制作ログ型と交互に試し、題材の需要差も記録したうえで比較する。

### タイトルは複数案を出して選ぶ（必須）

- タイトルは1案で決めない。4〜6案を作って並べ、オーナーが選ぶ。
- 各案に、字数（60字以内かどうか）・含まれる検索語・フックの角度（何で読ませるのか）を添える。
- 案の角度を散らす。同じ言い回しの語尾違いではなく、数字の対比／思い込みの否定／皮肉・意外な結末／短い断定形／検索語を厚くした形などを並べる。
- 書き手（Codex や Claude）が1案に決めて提出するのは禁止する。選ぶのはオーナーとする。

実例では、6案から `3バイト足りないだけで、PowerShell スクリプトはエラーも出さずに文字化けする` を採用した。43字と短い断定形で強く、「3バイト」という物理的な具体が効き、BOM という語を知らない読者にも届くためである。

### どちらの型でも守る共通条件

- タイトルは **60字以内**を目安にする（Zenn の表示上限）。
- 検索される人気 KW を1つ含める（例：Proxmox / 自宅サーバー / Linux）。
- 感情ワード（迷った、ハマった、気づいた）か、結果の約束（何が分かるか）のどちらかを入れる。
- 「初心者でも」「たった○分」などの敷居下げ表現は使わない。
- 競合名を出して攻撃的に対比しない。
- 実際にやったこと、起きたことから離れた強い表現は使わない。

---

## 3. 本文の構成（12点・★=必須）

1. ★**導入＝具体的な状況と痛み**から入る。抽象論・コマンド羅列で始めない。
2. ★**「この記事で分かること」（箇条書き3つ）**を、導入（痛み）の直後・最初の見出しより前に置く。
   - 各項目は1文（40〜60字程度）で書く。
   - 機能の列挙ではなく、読者が何をできるようになるかで書く。
   - 記事の中に実際にある内容だけを挙げ、書いていないことを約束しない。
3. ★**マクロ→ミクロ**：何を・なぜ、を先に置いてから手順へ。
4. ★**手順は「コマンド＋実際の出力ブロック」を必ずペア**で示す（再現可能に）。
5. ★**「なぜそうするか」を都度説明**。単なる手順書にしない（判断の根拠を書く）。
6. **mermaid 図**で対応関係・流れを可視化（`<br/>` を使う。`\n` は不可）。
7. **コールアウト**で注意を分離：`:::message`（補足）/ `:::message alert`（警告）。
8. ★**扱わない範囲を明示**してスコープを締める（広がりそうな所を切り出す）。
9. **補足セクション**で代替手段・脇道を「任意」に降格。
10. **実機検証ログ**を `:::details` アコーディオンに格納（※下のセキュリティ必須）。
11. ★**まとめは原則レベルの学び**（1文で言い切る）。
12. ★**フッター**（§5 を丸ごとコピペ・差し替え不要）。

トーン：制作ログ寄りの一人称・会話調。**断定しすぎず、不確実性に正直**。教科書解説ではなく「現場でどう判断したか」。

> **文体の正本は [`VOICE-PROFILE.md`](VOICE-PROFILE.md)**（人称・段落長の数値基準・必ず使う型5つ・禁止語）。
> このガイドは「何を書くか（構造）」、VOICE-PROFILE は「どう書くか（声）」。記事を書くときは両方読む。

---

## 4. 公開前チェック（push前に必須・lint）

このリポは public。**検出パターンの実値をここに書くとそれ自体が漏えいになる**ため、
チェックは非公開側の lint スクリプトに集約する。

```bash
bash outputs/scripts/qa/article-lint.sh zenn-content/articles/<slug>.md
```

lint は4層で見る。**BLOCK が 0 になるまで commit しない**（終了コード 1）。

| 層 | 内容 | 扱い |
|---|---|---|
| `[SEC]` | 公開してはいけない実値（内部IP・MAC・ホスト名・アカウント名・認証情報・内部URL） | BLOCK |
| `[JARGON]` | 外部読者に伝わらない内部前提（内部ID・内部ファイル名・内部呼称） | BLOCK |
| `[STYLE]` | AI 生成文の典型パターン（`VOICE-PROFILE.md` §5） | WARN |
| `[STRUCT]` | frontmatter 必須キー・コマンド/出力ペア・扱わない範囲・段落長 | WARN |

実機ログを載せる場合は、識別に不要な実値（IP/ユーザー名/MAC/ホスト名）を**伏せるか汎用化**してから `:::details` に入れる。

---

## 5. フッター雛形（コピペ用）

そのままコピーして使う。記事ごとに差し替える箇所は無い。

```markdown
---

## ご紹介

自分は、自宅に物理サーバーを置いて、インフラを学べる場所を作っています。

教材は登録なしで無料で読めます。実機のサーバーに接続する演習は構築中で、まだ全章には対応していません。

**全章インデックス:** [infra-study.org/curriculum](https://infra-study.org/curriculum)

**感想フォーム:** [感想フォーム（Tally）](https://tally.so/r/eqvX8l)

---

X（@taro3_01）で更新を告知しています。フォローすると次回の通知が届きます。

@[card](https://x.com/taro3_01)
```

> メモ：「おまけ」の文を含む既存記事は、記事ごとに個別に削除する。公開済み記事は旧フッター（主語のない文面・章への直リンク）のままであるため、あとでまとめて直す。

---

## 6. ファイル名（slug）規約

- `articles/<slug>.md`。slug は **英小文字・数字・ハイフン**、内容が分かる名前（例 `linux-man-command-offline-help`）。
- Zenn 制約：12〜50文字 `^[a-z0-9\-_]{12,50}$`。
- 追加したら `articles/INDEX.md` に wikilink を1行足す（内部管理用・`.gitignore` 済）。

---

## 7. チェックリスト（記事を出す前に）

- [ ] タイトルが §2 の型（**4〜6案を並べてオーナーが選んだもの**。書き手が1案で決めていない）
- [ ] frontmatter に `book_part`/`book_chapter`/`book_order` がある
- [ ] §3 の★必須を満たす（導入の痛み / **この記事で分かること3つ** / コマンド＋出力ペア / なぜ / 扱わない範囲 / まとめ / フッター）
- [ ] フッターを §5 のとおり入れた
- [ ] §4 セキュリティ grep がクリーン
- [ ] `published: false`（公開判断はオーナー）
